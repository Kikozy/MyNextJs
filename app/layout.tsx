import type { Metadata } from "next"
import dynamic from "next/dynamic"
import { Inter } from "next/font/google"
import Image from "next/image"
import "./globals.scss"
import { AppContextProvider } from "./components/AppContext"
import GlobalHeader from "./components/globalHeader/globalHeader"
const inter = Inter({ subsets: ["latin"] })

const PageLoading = dynamic(
	() => import("./components/pageLoading/pageLoading"), // 替换为你的组件路径
	{ ssr: false }
)

export const metadata: Metadata = {
	title: "Create Next App",
	description: "Generated by create next app",
}

function handleClick() {
	alert("花Q")
}

export default function RootLayout({
	children,
}: Readonly<{
	children: React.ReactNode
}>) {
	return (
		<html lang="zh">
			<head>
				<link rel="preload" href="/loading.gif" as="image" />
			</head>
			<body>
				<div className="loading-mask">
					<div className="loading-container">
						<div className="dot"></div>
						<div className="dot"></div>
						<div className="dot"></div>
					</div>
				</div>
				<AppContextProvider>
					<GlobalHeader />
					<div className="globalContent">
						<div className="content-core" onClick={handleClick}>
							<PageLoading>{children}</PageLoading>
						</div>
					</div>
				</AppContextProvider>
				<script
					dangerouslySetInnerHTML={{
						__html: `
              window.addEventListener('load',()=>{
								setTimeout(()=>{
									console.log('nmsl')
									const loadingMask = document.querySelector('.loading-mask');
									if(loadingMask)
										loadingMask.classList.add('hidden')
										// loadingMask.style.display = 'none'
								},1500)
							})
            `,
					}}
				></script>
			</body>
		</html>
	)
}
